# FraudShield.AI - Frontend

Современный веб-интерфейс для системы защиты от мошенничества на основе машинного обучения.

## 🚀 Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Структура проекта

```
frontend/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Корневой layout с навигацией
│   ├── page.tsx           # Главная страница (Dashboard)
│   ├── transactions/      # Страницы транзакций
│   ├── analytics/         # Аналитика
│   ├── customers/         # Управление клиентами
│   ├── evidence/          # Центр доказательств
│   ├── integrations/      # Интеграции
│   └── settings/          # Настройки
├── components/
│   ├── layout/            # Компоненты layout
│   │   ├── sidebar.tsx   # Боковое меню
│   │   └── header.tsx    # Верхний header
│   └── ui/                # UI компоненты
│       ├── button.tsx
│       ├── card.tsx
│       ├── badge.tsx
│       └── stat-card.tsx
├── lib/
│   ├── types.ts           # TypeScript интерфейсы
│   ├── utils.ts           # Вспомогательные функции
│   └── data-loader.ts     # Загрузка данных и ML скоринг
└── styles/
    └── globals.css        # Глобальные стили
```

## 🎨 Технологии

- **Next.js 15** - React framework с App Router
- **TypeScript** - Типизация
- **Tailwind CSS** - Утилитарный CSS фреймворк
- **Recharts** - Библиотека графиков
- **Lucide Icons** - Иконки
- **PapaCSV** - Парсинг CSV файлов

## 📊 Основные функции

### Dashboard (Главная)
- KPI карточки (fraud rate, заблокированные транзакции, сэкономленные средства)
- График динамики мошенничества за 7 дней
- Топ регионов с высоким риском
- Live-лента последних транзакций

### Транзакции
- Поиск по email, IP, ID транзакции
- Фильтрация по уровню риска
- Сортировка по дате, сумме, риску
- Детальная страница каждой транзакции

### Детали транзакции
- **Анимированный gauge риска** (0-100)
- **Факторы риска** - почему ML модель посчитала транзакцию подозрительной
- Информация о клиенте (история заказов, чарджбэки)
- Данные устройства и IP (VPN/Proxy детекция)
- Информация о доставке
- Кнопки действий (Одобрить/Блокировать/Проверка)

### Аналитика
- Графики fraud-attempts за 30 дней
- Распределение по устройствам
- Распределение по категориям товаров
- Анализ платежных шлюзов
- Метрики ML модели (Precision, Recall)

## 🧠 ML Risk Scoring

Система автоматически вычисляет **Risk Score (0-100)** на основе:

- Товар высокого риска (+20)
- 3DS не пройдена (+25)
- Множественные попытки оплаты (+10 каждая)
- VPN/Proxy/Tor (+30)
- Эмулятор устройства (+35)
- Несовпадение адресов (+15)
- Высокая velocity по карте/IP (+15-20)
- История чарджбэков (+30 за каждый)
- Телефон не подтвержден (+10)
- Новый email (<30 дней) (+15)

**Уровни риска:**
- 🔴 Высокий: ≥80
- 🟡 Средний: 50-79
- 🟢 Низкий: <50

## 💾 Интеграция данных

### Текущая реализация
Используются mock-данные, структурированные в соответствии с вашим CSV файлом.

### Подключение реальных данных

1. Скопируйте `ecommerce_fraud_dataset.csv` в `public/data/`

2. Обновите `lib/data-loader.ts`:

```typescript
export async function loadTransactions(): Promise<Transaction[]> {
  const response = await fetch('/data/ecommerce_fraud_dataset.csv');
  const csvText = await response.text();
  const parsed = Papa.parse<Transaction>(csvText, {
    header: true,
    dynamicTyping: true,
  });
  return parsed.data;
}
```

## 🎨 Дизайн-система

### Цвета

```css
/* Risk levels */
--risk-high: #ef4444     /* Красный */
--risk-medium: #f59e0b   /* Оранжевый */
--risk-low: #10b981      /* Зеленый */

/* Status */
--status-approved: #10b981
--status-blocked: #ef4444
--status-pending: #f59e0b
--status-review: #3b82f6
```

### Компоненты

Все компоненты поддерживают dark mode и имеют:
- Glassmorphic эффекты (backdrop-blur)
- Градиентные границы
- Плавные анимации
- Hover эффекты

## 🛠️ Команды

```bash
# Разработка
npm run dev          # Запуск dev сервера на http://localhost:3000

# Production
npm run build        # Сборка для продакшена
npm start            # Запуск production сервера

# Линтинг
npm run lint         # Проверка кода с ESLint
```

## 📱 Адаптивность

Интерфейс полностью адаптивен:
- **Desktop**: Полная навигация с sidebar
- **Tablet**: Компактная навигация
- **Mobile**: Hamburger menu, оптимизированные таблицы

## 🌍 Локализация

Весь UI на русском языке:
- Форматирование валюты (₽)
- Форматирование дат (ДД.ММ.ГГГГ ЧЧ:ММ)
- Относительное время ("только что", "5 мин назад")
- Телефоны в формате +7 (XXX) XXX-XX-XX

## 🔐 Безопасность

- TypeScript для предотвращения ошибок типов
- Client-side risk scoring (никакие данные не уходят на сервер)
- Маскировка чувствительных данных (номера карт)
- XSS защита через React

## 📈 Производительность

- **Code splitting** - автоматическое разделение кода
- **Lazy loading** - компоненты загружаются по требованию
- **Optimized images** - Next.js автоматически оптимизирует изображения
- **Client-side caching** - транзакции кэшируются в памяти

## 🐛 Отладка

### Проблемы с запуском

1. Убедитесь, что Node.js версии 18+
```bash
node --version
```

2. Удалите node_modules и переустановите
```bash
rm -rf node_modules package-lock.json
npm install
```

3. Очистите кэш Next.js
```bash
rm -rf .next
npm run dev
```

## 📝 Лицензия

Proprietary - FraudShield.AI

## 🤝 Поддержка

Для вопросов и предложений создавайте issue в репозитории или обращайтесь к команде разработки.

---

**Разработано с ❤️ для защиты вашего бизнеса от мошенничества**
